import { string } from 'prop-types';
import LocalizedStrings from 'react-native-localization';
import { en } from '../localization';

export const strings = new LocalizedStrings({ en });

console.log("stringsssssss", strings)
